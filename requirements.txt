# News Radar · Python 相依套件
# 安裝：pip install -r requirements.txt
# 建議在 venv 內安裝避免汙染 system Python

# ===== 核心 (Milestone 1) =====
feedparser==6.0.11        # RSS 解析
httpx==0.28.1             # 異步 HTTP client（取代 requests，原生支援 async）
requests>=2.32            # legacy token refresh and account-discovery helpers
trafilatura==1.12.2       # 網頁正文抽取（純化成 markdown）
ddgs==9.14.4              # Podcast/財報深度文的延伸證據搜尋（5–10 源）
beautifulsoup4==4.12.3    # 抓 og:image 等 meta 標籤
pydantic==2.9.2           # 結構化資料驗證
pyyaml==6.0.2             # config.yaml 解析
python-dotenv>=1.2.1,<2.0.0  # .env 載入（python-substack 0.1.21+ 要求 >=1.2.1，死 pin 會 ResolutionImpossible）
# ===== AI 處理 (Milestone 2) =====
# 兩個 Gemini SDK 並存：新版（google-genai）給 scorer/composer/analyst/reflector；
# 舊版（google-generativeai）給 competitor_agent。等 competitor_agent 遷移後可以刪掉舊版。
google-genai==1.73.1         # 新版 SDK，用法：from google import genai
google-generativeai==0.8.3   # 舊版 SDK，用法：import google.generativeai as genai
anthropic==0.39.0            # Claude API（週反思迭代用）
# ===== 審核 UI (Milestone 2) =====
fastapi==0.115.4
uvicorn[standard]==0.32.0
jinja2==3.1.4
# ===== 圖片處理 (Milestone 3) =====
Pillow==11.0.0
# ===== 工具 =====
rich==13.9.4              # 終端漂亮輸出（log / table）
python-dateutil==2.9.0    # 時區處理
tzdata>=2025.1            # zoneinfo database on Windows/minimal containers
pytest>=7.0
pytest-asyncio>=0.23
# ===== Cloud LLM routing (GitHub Actions, 2026-06-01) =====
litellm>=1.60.0            # Unified API for 100+ LLM providers
# ===== GitHub Actions deployment =====
pygithub>=2.5.0           # GitHub API wrapper for repo management
youtube-transcript-api>=1.2  # YouTube 逐字稿（1.x：0.6.x 的 fetch 因 YT 端點改版已壞）
# ===== Reels/Short Video Generation (2026-06-03) =====
edge-tts>=6.1.0           # Microsoft Edge TTS - free Chinese voiceover, no API key needed
moviepy>=2.1.0
matplotlib>=3.8.0
numpy>=1.24.0            # Video compositing (text + images + audio -> MP4)
# ===== Substack draft API — moved to requirements-mac.txt (2026-05-13) =====
# python-substack 只有 Mac 端 tools/substack_compose.py 會用到。Cloud GH Actions
# 的 publish_queue.yml / reflect_*.yml 等 workflows 完全用不到、且 v0.1.18 在
# Linux runner 上 install 會 fail，拖垮全部 cloud cron。
#
# 安裝指令：
#   Mac:    pip install -r requirements.txt -r requirements-mac.txt
#   Cloud:  pip install -r requirements.txt           ← 不裝 requirements-mac.txt
opencc>=1.3.1
python-multipart==0.0.12
yfinance

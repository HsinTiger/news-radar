# News Radar · Python 相依套件
# 安裝：pip install -r requirements.txt
# 建議在 venv 內安裝避免汙染 system Python

# ===== 核心 (Milestone 1) =====
feedparser==6.0.11        # RSS 解析
httpx==0.27.2             # 異步 HTTP client（取代 requests，原生支援 async）
trafilatura==1.12.2       # 網頁正文抽取（純化成 markdown）
beautifulsoup4==4.12.3    # 抓 og:image 等 meta 標籤
pydantic==2.9.2           # 結構化資料驗證
pyyaml==6.0.2             # config.yaml 解析
python-dotenv==1.0.1      # .env 載入

# ===== AI 處理 (Milestone 2) =====
google-generativeai==0.8.3   # Gemini API
anthropic==0.39.0            # Claude API（週反思迭代用）

# ===== 審核 UI (Milestone 2) =====
fastapi==0.115.4
uvicorn[standard]==0.32.0
jinja2==3.1.4

# ===== 圖片處理 (Milestone 3) =====
Pillow==11.0.0

# ===== 工具 =====
rich==13.9.4              # 終端漂亮輸出（log / table）
python-dateutil==2.9.0    # 時區處理
